eureka:
  client:
    register-with-eureka: false
spring:
  cloud:
    gateway:
      routes:
#        - id: auth-service
#          uri: http://localhost:8081
#          predicates:
#            - Path=/auth/**
        - id: chambre
          uri: http://localhost:8082
          predicates:
            - Path=/chambres/**
  application:
    name: gateway
server:
  port: 8222
